# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: ABQ Vets | Vets Helping Vets
email: mike@abqvets.com
description: >- # this means to ignore newlines until "baseurl:"
  Abqvets.com - Veteran resources providedby veterans. 
  Assistance with perks, benefits, tips and tricks to get the most out of your veteran status.
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://abqvets.com"
twitter_username: 
github_username:  
facebook_username:
instagram_username:

# Build settings
theme: minima
plugins:
  - jekyll-feed

# include:
#  - _includes/vetresources
#  - _includes/elements
# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#

# Set defaults for static files
defaults:
  - scope:
      path: assets/aftercare/pdf
    values: 
      image: false
      file: true
      pdf: true 
      source: false
  - scope:
      path: assets/aftercare/source
    values: 
      image: false
      file: true
      pdf: false 
      source: true
  - scope:
      path: assets/img 
    values: 
      image: true
      file: true
      pdf: false
      source: false


exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - .DS_Store
  - _data/old_data_bak
  - templatesSnippets/
  - ignore/
